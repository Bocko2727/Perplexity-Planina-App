import React from "react";
import ReactDOM from "react-dom/client";
import PlananaApp from "./PlananaApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PlananaApp />
  </React.StrictMode>
);
